
<div class="col-xs-12 col-sm-4">
  <%= link_to(c) do  %>
    <div class="card">
      <%= image_tag c.cocktailbanner.url(:card), {:class => "card-img"} %>
      <div class="card-description">
        <h2><%= c.name %></h2>
      </div>
    </div>
  <% end %>
</div>

